eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
spring:
  application:
    name: deanonym-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIGSERVER
      failFast: true
